# Optional drafts — do not auto-post

Review each venue's current rules and disclose authorship. These are prepared
copy, not evidence of acceptance, indexing, users or growth.

## Short announcement

I built a small GitHub launch skill for coding agents. It turns an existing
project into search-aware metadata, a factual README and an explicit upload
handoff. The optional Python checker is read-only and has no dependencies.
It doesn't promise stars or run promotional automation. I'd appreciate feedback
on whether the first-use and upload instructions work on a real project.

## Intended repository description

Use the approved description in metadata.json, not a keyword-stuffed sentence.

## Three appropriate early requests

Ask a few willing developers to try one existing project. Observe whether they
can get a usable README, understand the upload boundaries and complete the first
use without the author. Keep feedback private unless explicitly approved.

Offer a directory entry only where publishing/release skills are genuinely in
scope and follow the directory's contribution process. No bulk submission.

Publish a sanitized self-application example only after the repository is live.
The package's current synthetic fixtures are not a success story.
